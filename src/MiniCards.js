import React from 'react'

function MiniCards({words}) {
    const keys = Object.keys(words)
    const miniCard = 'hell'
    return (
        <div>
            
        </div>
    )
}

export default MiniCards
